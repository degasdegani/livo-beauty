# Asaas (billing / assinaturas) — ver src/lib/asaas.ts e src/lib/billing/

ASAAS_API_KEY=
ASAAS_ENV=sandbox
# Mesmo valor cadastrado no painel Asaas (Integracoes > Webhooks > authToken).
# Gerar com: openssl rand -hex 32
ASAAS_WEBHOOK_TOKEN=
